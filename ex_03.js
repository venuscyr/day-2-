function changebackground() {
  var magicNumber = 1;
  var magicNumber = 2;
  var magicNumber = 3;

  if (magicNumber === 3) {
    paintRedRectangle();
  }
 if (magicNumber === 3) {
    paintYellowRectangle();
  }
   if (magicNumber === 3) {
    paintGreenRectangle();
  }

  
}

changebackground();
